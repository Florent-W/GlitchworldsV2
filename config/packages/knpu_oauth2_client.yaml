knpu_oauth2_client:
    clients:
        oauth_google:
            type: google
            client_id: '%env(OAUTH_GOOGLE_ID)%'
            client_secret: '%env(OAUTH_GOOGLE_SECRET)%'
            redirect_route: app_oauth_verification
            redirect_params: { provider: google }
