{% extends 'base.html.twig' %}

{% block title %}Modifier mon commentaire - GlitchWorlds{% endblock %}
{% block robots_meta %}noindex,nofollow{% endblock %}

{% block body %}
<section class="gw-panel p-4 mx-auto" style="max-width: 760px">
    <h1 class="h2">Modifier mon commentaire</h1>
    {{ form_start(formulaire) }}
        {{ form_row(formulaire.contenu) }}
        {{ form_widget(formulaire.publier) }}
    {{ form_end(formulaire) }}
</section>
{% endblock %}
